<div class="row ml-0">
  <%= form_with model: event, local: true do |form| %>
    <%= errors_for event %>
    <div class="form-group">
      <%= form.label :title %>
      <%= form.text_field :title, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= form.label :description %>
      <%= form.text_area :description, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= form.label :address %>
      <%= form.text_field :address, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= form.label :flayer %>
      <%= form.file_field :flayer, class: "form-control-file" %>
    </div>
    <div class="card">
      <div class="card-body">
        <h5 class="card-title"><%= event_dates_section_title %></h5>
        <p><%= event_dates_instruction_text %></p>
        <%=form.fields_for :event_dates do |event_date_form| %>
          <div id="event-dates-nested-forms" class="form-group">
            <%= event_date_form.label :start_date %>
            <%= event_date_form.datetime_local_field :start_date, class: "form-control" %>
            <%= event_date_form.label :end_date %>
            <%= event_date_form.datetime_local_field :end_date, class: "form-control" %>
            <% if action_name == 'edit' %>
              <%= event_date_form.label "Delete"%>
              <%= event_date_form.check_box :_destroy %>
            <% end %>
            <hr>
          </div>
        <% end %>
        <% if action_name == 'new' %>
          <%= link_to "Add Date", new_event_event_date_path_path,
                      class: [ 'btn', 'btn-primary'], remote: true %>
        <% end %>
      </div>
    </div>
    <div class="form-group mt-2">
      <%= form.submit submit_btn_text, class: ["btn", "btn-primary"] %>
      <%= link_to "Back", :back, class: ["btn", "btn-secondary"] %>
    </div>
  <% end %>
</div>